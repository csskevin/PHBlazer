---
title: Well
domain: https://your-domain
---
{{{ template.[default/header] }}}

# Hello World

This could be your main page. You can use all meta informations from all posts.
e.g. to use a global domain variable you can define it in `index.md` and access
it by `content.index.meta.domain` (see e.g. in `rss.xml`).

{{{ template.[default/footer] }}}
